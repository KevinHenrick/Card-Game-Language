SETUP{
	list hand=[];
	list deck=[$AH, $KH];
	hand <+ <- deck;
	card a1=hand->;
	card b1=deck->;
	string a=intToString(value(a1))^suit(a1);
	string b=intToString(value(b1))^suit(b1);
	print(a^"\n"^b);
	
}
